# Bot Configuration
# These environment variables override config.json settings

# Minecraft Server connection (use MC_ prefix to avoid conflict with web PORT)
MC_HOST=your-server.aternos.me
MC_PORT=25565
BOT_USERNAME=YourBotName

# API Authentication (REQUIRED - change this!)
API_KEY=your-secret-key-here

# Web server port (for Render/hosting)
# PORT=5000

# Retry settings (in milliseconds)
# RETRY_DELAY=15000
# INITIAL_RETRY_DELAY=60000
